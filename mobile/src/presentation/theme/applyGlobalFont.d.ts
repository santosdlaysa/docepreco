/**
 * Declaração de tipos para o módulo com variantes por plataforma
 * (applyGlobalFont.native.tsx / applyGlobalFont.web.ts). O Metro escolhe a
 * implementação certa em build; o TypeScript usa esta declaração.
 */
export declare function applyGlobalFont(): void;
